{
  "name": "frontend-server",
  "version": "1.0.0",
  "description": "Servidor frontend para servir arquivos estáticos",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "cross-env NODE_ENV=development nodemon server.js",
    "prod": "cross-env NODE_ENV=production node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "express",
    "frontend",
    "server",
    "static"
  ],
  "license": "MIT"
}
